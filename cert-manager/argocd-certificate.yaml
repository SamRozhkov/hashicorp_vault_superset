apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argocd-local-cert
  namespace: argocd
spec:
  secretName: argocd-local-tls
  dnsNames:
    - argocd.local
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer
