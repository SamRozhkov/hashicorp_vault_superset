apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: openmetadata-local-cert
  namespace: openmetadata
spec:
  secretName: openmetadata-local-tls
  dnsNames:
    - openmetadata.rozhkov.pro
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer
